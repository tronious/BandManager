# Backend Environment Variables
# Copy this to .env and fill in your values

# Server port (Azure will override this)
PORT=8080

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# API Key for frontend authentication
API_KEY=your-api-key-here

# Admin PIN for admin endpoints (sent as the `x-admin-password` header)
# Recommended: numeric-only so it works nicely on mobile.
# Example: ADMIN_PIN=3381975
ADMIN_PIN=3381975

# Supabase connection (use SERVICE KEY here, not anon key!)
SUPABASE_URL=your-supabase-url-here
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Email settings for booking notifications (Gmail)
# Create an App Password at: https://myaccount.google.com/apppasswords
EMAIL_USER=your-gmail-address@gmail.com
EMAIL_APP_PASSWORD=your-gmail-app-password
